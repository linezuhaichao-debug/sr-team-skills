# 功能 GDD：<功能名>

> **用途**: <交给程序实现依据的最终定稿；内部留档，不外发>
> **详细度铁律**: 本模板每一节都要求"详细"。功能规则、界面流程、配置契约、验收标准写到程序可实现的粒度；数值能引配置字段就引，否则标"待配表"；宁可详尽，不要省略。
> **过程留痕边界**: 证据标注、假设/风险台账、设计核与取舍过程**留在 sr-gdd-human 成稿中**，本模板只吸收结论——交付给程序的是没有思考决策过程的干净 GDD。
> **主线路径（输入含 sr-gdd-human 成稿时）**：§1–§5 从成稿对应章节逐章吸收（内容不重写、口径不改动，只做配置字段对应），本步重心是 §6 配置契约、§7 多语言文本、§8 验收标准；只有直接从旧案/配置表起步（直接调用路径）时，§1–§5 才由本步全新撰写。

---

## 0. 来源材料清单（内部留档，不随交付外发）

| 材料 | 状态 | 用途 |
| --- | --- | --- |
| <sr-gdd-human 成稿 / 旧策划案 / 配置表变更 / 决议记录> | <高置信 / 部分 / 未读> | <本文档哪些章节依据它> |

## 1. 功能意图

### 一句话功能

<一段话说清：这是什么系统，玩家做什么，得到什么>

### 为什么要做

<现状问题 + 本次改造的核心价值；不是堆功能，而是改变什么玩家行为>

### 本功能不解决什么

| 不解决项 | 原因 |
| --- | --- |
| <明确排除的范围> | <对应决议或理由> |

## 2. 玩家动词清单

| 动词类型 | 玩家动作 |
| --- | --- |
| 直接动作 | |
| 战斗/对局动作 | |
| 养成动作 | |
| 脑内判断 | |
| 重复行为 | |
| 80% 时间重复行为 | |

## 3. 核心循环

### 即时循环

```text
<动作 -> 反馈 -> 奖励>
```

### 单次会话循环

```text

```

### 长期循环

```text

```

## 4. 功能规则

> 每条规则一个 R 编号，写到程序可实现的粒度（条件/动作/结果/边界齐全）。
> 输入材料薄时不得编造——先走"决议问答"补齐取舍（见 sr-gdd-ai SKILL 第 2 步）。
> 主线路径：以 human 成稿的规则组为基底吸收（内容不重写、口径不改动，只把机制表述对应到 `表名.字段名`），规则编号沿用成稿组号。
> 详细度：数值标 `表名.字段名` 或"待配表"；来源标注与拍板留痕在成稿中，本模板不携带。

### R1. <规则名>

| 规则 | 细节 |
| --- | --- |
| <规则项> | <条件/动作/结果/边界 + 配置字段对应> |

## 5. 用户体验与界面流程

> **详细度要求（必做）**：为**每个关键界面**单独给出——
> ① ASCII 线框图（含布局、控件、UE 交互标注）；② UE 规则表（逐条编号 UE1…，写清交互/反馈/边界）；
> ③ 界面清单总览表；④ 流程 text 图；⑤ 状态机（如适用）；⑥ 全局交互规范。
> 每个界面单独出线框图（层级总图代替不了单界面图）。UE = 用户体验/交互规则（User Experience）。

### 5.0 界面清单总览

| 界面ID | 名称 | 进入方式 | 退出方式 |
| --- | --- | --- | --- |
| UI-M1 | <界面名> | <进入方式> | <退出方式> |

### 5.1 UI-M1 <界面名>

```text
<ASCII 线框图：布局 + 控件 + UE 标注（标注格式：UE1 xxx）>
```

| 编号 | 规则（详细） |
| --- | --- |
| UE1 | <交互规则：对象/条件/动作/反馈/边界> |

### 5.2 UI-M2 <界面名>

```text
<ASCII 线框图>
```

| 编号 | 规则（详细） |
| --- | --- |
| UE1 | <交互规则> |

### 5.N 界面流程（text）

```text
<从入口到结束的完整操作序列 text 图>
```

### 5.N+1 状态机（如适用）

```text
<状态流转 text 图>
```

### 5.N+2 全局交互规范

| 规范 | 规则 |
| --- | --- |
| <规范项> | <规则> |

## 6. 配置契约

> 每张表列：中文字段 / 英文字段 / 数据结构 / 含义 / 状态（已有 / 新增 / 新增命名待定）。
> 与 sr-config 配置变更一一对应；规则章的每个 `表名.字段名` 都能在本节找到定义。

## 7. 多语言文本

> 本功能涉及的多语言键，按项目通用格式：名称 `n{id}`、描述 `d{id}`（`id` 取实体配置主键，如英雄 `gid`）。
> 与 sr-gtgenerator 的 gtypes/normaltxt 变更对应；新键列出键名、中文文案与状态（已有 / 新增 / 待配）。

| 键 | 中文 | 状态 |
| --- | --- | --- |
| n<id> | <名称文案> | 已有 / 新增 |
| d<id> | <描述文案> | 已有 / 新增 |

## 8. 验收标准

| 编号 | 标准 | 优先级 |
| --- | --- | --- |
| AC1 | | P0/P1 |

## 9. 交接清单

| 角色 | 需要内容 | 负责人 |
| --- | --- | --- |
| 战斗程序 | | 待定 |
| 服务端程序 | | 待定 |
| UI 程序 | | 待定 |
| UI/UX | | 待定 |
| 美术 | | 待定 |
| 配置 | | 待定 |
| 测试 | | 待定 |
