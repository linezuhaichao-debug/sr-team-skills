# 裸调用行为（各 sr 工作流 skill 共用）

不带任何参数或主题调用（"裸 `/sr-xxx`"）时，遵守同一条纪律：**先用一段话向用户说明本 skill 需要什么输入，然后停下等用户补齐**；用户补齐后再从该 skill 的第 0 步开始。在收集到输入前，不执行流程、不自行扫描 workspace 找题目、不基于猜测的题目开工。

各 skill 需要收集的输入清单见其 SKILL.md「无输入时的行为」节——那是各 skill 的专属差异，本文件只承载共用纪律。
